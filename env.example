# Database configures
DB_HOST=localhost
DB_USER=username
DB_PASS=password
DB_NAME=databaseName
DB_CHAR=utf8mb4
DB_PORT=3306
PASS_HASH_ALG=PASSWORD_DEFAULT
TEXT_HASH_ALG=sha256

# Number of fake users to be feeded to the users table
NUM_USERS=10000

# JWT secret key to encoding and decoding
JWT_SECRET="fkjajefskdajfes"

# Redis configure
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_DATABASE=0
REDIS_READ_WRITE_TIMEOUT=0
REDIS_USERNAME='root'
REDIS_PASSWORD=''
REDIS_TIMEOUT=0.0
REDIS_SCHEME=tcp

# Path to the file that use to get token from the firebase to use it in creating push notification
GOOGLE_APPLICATION_CREDENTIALS="serverAuthentication.json"


